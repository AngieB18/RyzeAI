import 'package:flutter/material.dart';

class HomePublicationIcon extends StatelessWidget {
  const HomePublicationIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.article_rounded);
  }
}
